# better-auth pulls transitive packages that declare Node >=22 in their
# engines field (kysely, @testing-library/jest-dom). None of them run in the
# browser bundle, and this project targets Node 20, so the advisory check is
# turned off rather than pinning each one.
--ignore-engines true
